/*
#include "QuadEncoders.h"
#include "../RobotMap.h"

QuadEncoders::QuadEncoders() :
		Subsystem("ExampleSubsystem")
{
	LeftEncoder1 = new Encoder(Enc_Lt_A);
	LeftEncoder2 = new Encoder(Enc_Lt_B);
	RightEncoder1 = new Encoder(Enc_Rt_A);
	RightEncoder2 = new Encoder(Enc_Rt_B);
}

void QuadEncoders::InitDefaultCommand()
{
	// Set the default command for a subsystem here.
	//SetDefaultCommand(new MySpecialCommand());
}

// Put methods for controlling this subsystem
// here. Call these from Commands.
*/
